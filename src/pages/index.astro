---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import Button from '../components/Button.astro';
import SectionLabel from '../components/SectionLabel.astro';
import ProjectCard from '../components/ProjectCard.astro';
import OfferingRow from '../components/OfferingRow.astro';
import PostRow from '../components/PostRow.astro';
import FaqItem from '../components/FaqItem.astro';
import TechTile from '../components/TechTile.astro';
import NodeNetwork from '../components/NodeNetwork.astro';
import { getCollection } from 'astro:content';
import {
  stats,
  services,
  posts,
  writingIndexHref,
  faqs,
  tech,
  socials,
  bookingHref,
} from '../data/site';

/* Same destinations as the nav, minus the CTA: the footer restates the page. */
const footerNav = [
  { label: 'Work', href: '#work' },
  { label: 'How I help', href: '#services' },
  { label: 'FAQ', href: '#faq' },
  { label: 'About', href: '#about' },
];

/* Only real case studies go in the grid — no placeholder cards. Newest first,
   capped at 3: the grid is a 2+1 layout and a fourth card breaks it. */
const projects = (await getCollection('projects'))
  .sort((a, b) => b.data.year - a.data.year)
  .map((entry) => ({
    name: entry.data.title,
    description: entry.data.subtitle,
    href: `/projects/${entry.id}`,
    meta: entry.data.meta,
    bucket: entry.data.bucket,
    stack: entry.data.stack,
    linkLabel: 'Read case study',
  }))
  .slice(0, 3);
---

<BaseLayout>
  <div id="top" class="page">
    <Nav />

    <!-- Hero -->
    <section class="hero">
      <div class="hero-copy">
        <h1 class="hero-title">
          I build AI systems that make businesses <span class="accent">money</span>.
        </h1>
        <p class="hero-sub">
          More customers. More revenue from each one. Fewer hours lost to manual work.
        </p>
        <div>
          <Button href={bookingHref} arrow fillHover>Book a call</Button>
        </div>
      </div>
      <div class="hero-visual">
        <NodeNetwork />
        <div class="tag tag-acquire">Acquire<span class="tag-line"></span></div>
        <div class="tag tag-retain"><span class="tag-line"></span>Retain</div>
        <div class="tag tag-automate">Automate</div>
      </div>
    </section>

    <!-- Stats -->
    <section class="stats section-inner" aria-label="Key numbers">
      <dl class="stats-grid">
        {
          stats.map((s) => (
            <div class="stat">
              <dt class:list={['stat-value', s.words && 'stat-value--words']}>{s.value}</dt>
              <dd class="stat-label">{s.label}</dd>
            </div>
          ))
        }
      </dl>
    </section>

    <!-- Selected work -->
    <section id="work" class="projects section-inner">
      <div class="lead">
        <SectionLabel as="h2">Selected work</SectionLabel>
      </div>
      <div class="projects-grid">
        {
          projects.map((p, i) => (
            <ProjectCard
              name={p.name}
              description={p.description}
              href={p.href}
              meta={p.meta}
              bucket={p.bucket}
              stack={p.stack}
              linkLabel={p.linkLabel}
              wide={i === projects.length - 1 && projects.length % 2 === 1}
            />
          ))
        }
      </div>
    </section>

    <!-- How I help -->
    <section id="services" class="offerings section-inner">
      <div class="lead">
        <SectionLabel as="h2">How I help</SectionLabel>
      </div>
      <div class="offerings-list">
        {
          services.map((s) => (
            <OfferingRow
              name={s.name}
              description={s.description}
              href={s.href}
              linkLabel={s.linkLabel}
            />
          ))
        }
      </div>
    </section>

    <!-- Recent writing -->
    {
      posts.length > 0 && (
        <section id="writing" class="writing section-inner">
          <div class="lead">
            <SectionLabel as="h2">Recent writing</SectionLabel>
          </div>
          <div class="writing-list">
            {posts.map((p) => (
              <PostRow title={p.title} href={p.href} category={p.category} date={p.date} />
            ))}
          </div>
          <a href={writingIndexHref} class="text-link writing-all">
            All writing <span class="accent">&rarr;</span>
          </a>
        </section>
      )
    }

    <!-- FAQ -->
    {
      faqs.length > 0 && (
        <section id="faq" class="faq-sec section-inner">
          <div class="lead">
            <SectionLabel as="h2">FAQ</SectionLabel>
          </div>
          <div class="faq-list">
            {faqs.map((f) => (
              <FaqItem question={f.question} answer={f.answer} />
            ))}
          </div>
        </section>
      )
    }

    <!-- About -->
    <section id="about" class="about">
      <div class="about-inner">
        <div class="about-head">
          <SectionLabel>About me</SectionLabel>
          <h2 class="h2">Engineer by background.<br />Builder by choice.</h2>
        </div>
        <div class="about-copy">
          <p class="body-sm">
            Twelve years as a software engineer, shipping production applications where
            uptime, security and maintenance actually mattered. Now I point that at AI.
          </p>
          <p class="body-sm">
            It shows in what I build: systems that know when they're unsure, recover
            when an API fails, and can be audited when someone asks why they did
            something.
          </p>
          <p class="body-sm">Based in Morocco. Clients across Europe and the US.</p>
        </div>
      </div>
    </section>

    <!-- Tech stack -->
    <section class="tech">
      <div class="section-inner">
        <div class="lead">
          <SectionLabel as="h2" color="var(--text-muted)">Tech stack</SectionLabel>
        </div>
        <div class="tech-grid">
          {tech.map((t) => <TechTile slug={t.slug} label={t.label} size={64} />)}
        </div>
      </div>
    </section>

    <!-- CTA footer -->
    <footer id="contact" class="footer">
      <div class="footer-top section-inner">
        <h2 class="footer-title">
          Ready to build your<br /><span class="accent">next system</span>?
        </h2>
        <div class="footer-cta">
          <p class="body-sm">
            Bring the workflow that's eating your team's time. Thirty minutes is enough
            to know if it's worth automating.
          </p>
          <div>
            <Button href={bookingHref}>Book a call</Button>
          </div>
        </div>
      </div>

      <div class="footer-map section-inner">
        <nav class="foot-nav" aria-label="Footer">
          {footerNav.map((l) => <a href={l.href}>{l.label}</a>)}
        </nav>

        <div class="foot-cols">
          <div class="foot-col">
            <h3 class="foot-col-title">Case studies</h3>
            {projects.map((p) => <a href={p.href}>{p.name}</a>)}
          </div>
          <div class="foot-col">
            <h3 class="foot-col-title">Elsewhere</h3>
            {/* Off-site profiles open in a tab of their own, so checking the
                Upwork reviews does not cost you the page. */}
            {socials.map((s) => (
              <a
                href={s.href}
                target={s.href.startsWith('http') ? '_blank' : undefined}
                rel={s.href.startsWith('http') ? 'noopener' : undefined}
              >
                {s.label}
              </a>
            ))}
          </div>
          <p class="foot-blurb">
            AI systems that make businesses money. Based in Morocco, working with
            clients across Europe and the US.
          </p>
        </div>
      </div>

      <!-- Decorative: the brand is already named in the nav and the copyright. -->
      <div class="wordmark" aria-hidden="true">zakadev</div>

      <div class="footer-bar">
        <div class="footer-bar-inner section-inner">
          <span>&copy; 2026 zakadev. All rights reserved.</span>
          <span>Built with Astro. No trackers, no cookie banner.</span>
        </div>
      </div>
    </footer>
  </div>
</BaseLayout>

<style>
  .page {
    min-height: 100vh;
  }

  /* Shared container. Vertical padding lives on each section. */
  .section-inner {
    max-width: var(--container-max);
    margin: 0 auto;
    padding-left: var(--container-pad);
    padding-right: var(--container-pad);
  }

  .accent {
    color: var(--orange);
  }

  .h2 {
    font-family: var(--font-serif);
    font-weight: 400;
    font-size: clamp(30px, 4.4vw, 38px);
    line-height: 1.26;
    letter-spacing: var(--tracking-h2);
  }

  .body-sm {
    font: 400 15px/1.73 var(--font-sans);
    color: var(--text-secondary);
  }

  .text-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: var(--text-primary);
    font: 500 14px/1 var(--font-sans);
    width: fit-content;
  }

  .text-link .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--orange);
    flex: none;
  }

  .lead {
    margin-bottom: 32px;
  }

  /* ---------- Hero ---------- */
  /* The claim is the hero, so the copy column takes the larger share. The
     visual shrinks first as the viewport narrows; the headline never does. */
  /* Fixed proportions, not flex-basis negotiation: the copy column has to stay
     wide enough for "that make businesses" to hold one line. */
  .hero {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 56px var(--container-pad) 48px;
    display: grid;
    grid-template-columns: 1.55fr 1fr;
    align-items: center;
    gap: 56px;
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    gap: 32px;
    min-width: 0;
  }

  /* No hard line breaks: the measure sets the rag, so the headline holds its
     shape at every width instead of snapping at the ones we guessed. */
  .hero-title {
    font-family: var(--font-serif);
    font-weight: 400;
    font-size: clamp(38px, 4.6vw, 62px);
    line-height: 1.1;
    letter-spacing: var(--tracking-display);
    max-width: 22ch;
    /* Evens the rag so the line does not end on "that". */
    text-wrap: balance;
  }

  .hero-sub {
    font: 400 20px/1.6 var(--font-sans);
    color: var(--text-secondary);
    max-width: 46ch;
  }

  .hero-visual {
    position: relative;
    aspect-ratio: 4 / 3;
    min-width: 0;
  }

  .tag {
    position: absolute;
    display: flex;
    align-items: center;
    gap: 8px;
    font: var(--type-caption);
    letter-spacing: var(--tracking-caption);
    text-transform: uppercase;
    color: var(--text-secondary);
  }
  .tag-line {
    width: 40px;
    height: 1px;
    background: var(--border-hover);
  }
  /* Percentages, not pixels: the visual is fluid now, so the labels have to
     ride it rather than sit at coordinates that were true at 560px. */
  .tag-acquire {
    top: 6%;
    left: 21%;
  }
  .tag-retain {
    top: 40%;
    right: 0;
  }
  .tag-automate {
    bottom: 3%;
    left: 36%;
  }

  /* ---------- Stats ---------- */
  .stats {
    padding-top: 8px;
    padding-bottom: 56px;
  }
  .stats-grid {
    margin: 0;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    border-top: 1px solid var(--border-1);
  }
  .stat {
    padding: 32px 32px 32px 0;
    border-left: 1px solid var(--border-1);
    padding-left: 32px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .stat:first-child {
    border-left: none;
    padding-left: 0;
  }
  /* Values sit on a shared baseline band, so a smaller word-value still leaves
     every label on the same line across the row. */
  .stat-value {
    font: 400 clamp(34px, 4vw, 44px) / 1.05 var(--font-serif);
    letter-spacing: var(--tracking-h2);
    color: var(--text-primary);
    display: flex;
    align-items: flex-end;
    min-height: calc(clamp(34px, 4vw, 44px) * 1.05);
  }

  /* A word-value at number size wraps and drags the row out of alignment. */
  .stat-value--words {
    font-size: clamp(22px, 2.2vw, 26px);
  }
  .stat-label {
    margin: 0;
    font: var(--type-caption);
    letter-spacing: var(--tracking-caption);
    text-transform: uppercase;
    color: var(--text-muted);
  }

  /* ---------- About ---------- */
  .about {
    background: var(--bg-secondary);
  }
  /* Heading holds the left, prose answers on the right. The photo slot is gone,
     so the asymmetry has to come from the type instead. */
  .about-inner {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 104px var(--container-pad);
    display: grid;
    grid-template-columns: 5fr 6fr;
    gap: 64px;
    align-items: start;
  }
  .about-head {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }
  .about-copy {
    display: flex;
    flex-direction: column;
    gap: 20px;
    max-width: 62ch;
    /* Optically aligns the first line with the heading's cap height. */
    padding-top: 6px;
  }

  /* ---------- Selected work ---------- */
  .projects {
    padding-top: 88px;
    padding-bottom: 104px;
  }
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
  }

  /* ---------- How I help ---------- */
  .offerings {
    padding-top: 24px;
    padding-bottom: 104px;
  }
  .offerings-list {
    border-top: 1px solid var(--border-1);
  }

  /* ---------- Recent writing ---------- */
  .writing {
    padding-top: 40px;
    padding-bottom: 120px;
  }
  .writing-list {
    border-top: 1px solid var(--border-1);
  }
  .writing-all {
    margin-top: 32px;
  }

  /* ---------- FAQ ---------- */
  .faq-sec {
    padding-top: 24px;
    padding-bottom: 112px;
  }
  .faq-list {
    border-top: 1px solid var(--border-1);
  }


  /* ---------- Tech ---------- */
  .tech {
    background: var(--bg-secondary);
    padding: 96px 0;
  }
  .tech-grid {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  /* ---------- Footer ---------- */
  /* Sizing context for the full-bleed wordmark below. */
  .footer {
    container-type: inline-size;
  }

  .footer-top {
    padding-top: 120px;
    padding-bottom: 88px;
    display: grid;
    grid-template-columns: 6fr 5fr;
    gap: 48px;
    align-items: center;
  }
  .footer-title {
    font-family: var(--font-serif);
    font-weight: 400;
    font-size: clamp(36px, 5.6vw, 52px);
    line-height: 1.19;
    letter-spacing: var(--tracking-h1);
  }
  .footer-cta {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
  /* ---------- Footer site map ---------- */
  .footer-map {
    padding-bottom: 72px;
    border-top: 1px solid var(--border-1);
    padding-top: 48px;
  }

  /* The page's four destinations, restated at display size and spread edge to
     edge. This is the row that makes the footer read as a place, not a margin. */
  .foot-nav {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 24px 40px;
    margin-bottom: 64px;
  }
  .foot-nav a {
    font-family: var(--font-serif);
    font-weight: 400;
    font-size: clamp(26px, 3.6vw, 44px);
    line-height: 1.1;
    letter-spacing: var(--tracking-h2);
    color: var(--text-primary);
  }
  .foot-nav a:hover {
    color: var(--orange);
  }

  .foot-cols {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr)) minmax(0, 1.4fr);
    gap: 40px;
  }
  .foot-col {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }
  .foot-col-title {
    font: var(--type-caption);
    letter-spacing: var(--tracking-caption);
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 6px;
  }
  .foot-col a {
    font: var(--type-small);
    color: var(--text-secondary);
  }
  .foot-col a:hover {
    color: var(--text-primary);
  }
  .foot-blurb {
    font: var(--type-small);
    color: var(--text-muted);
    max-width: 42ch;
    justify-self: end;
  }

  /* Full-bleed brand line. The size is derived, not guessed: "zakadev" in Sora
     at this tracking runs ~4.1x its own font size, so width / 4.1 fills the
     container exactly, and the vw term keeps that true below the max width. */
  .wordmark {
    font-family: var(--font-serif);
    font-weight: 400;
    /* Measured: the word renders 3.75x its font size at this tracking. Container
       units, not vw, so a desktop scrollbar cannot clip the last letter. */
    font-size: min(calc((100cqw - 2 * var(--container-pad)) / 3.75), 341px);
    line-height: 0.86;
    letter-spacing: -0.045em;
    text-align: center;
    color: var(--text-primary);
    padding: 0 var(--container-pad);
    overflow: hidden;
    user-select: none;
  }

  .footer-bar {
    border-top: 1px solid var(--border-1);
  }
  .footer-bar-inner {
    padding-top: 24px;
    padding-bottom: 24px;
    display: flex;
    justify-content: space-between;
    gap: 16px;
    font: var(--type-caption);
    color: var(--text-muted);
    letter-spacing: 0.04em;
  }

  /* ---------- Responsive ---------- */
  @media (max-width: 960px) {
    .footer-top {
      grid-template-columns: 1fr;
      gap: 32px;
    }
    .foot-cols {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
    .foot-blurb {
      grid-column: 1 / -1;
      justify-self: start;
      max-width: none;
    }
  }

  @media (max-width: 900px) {
    .hero {
      grid-template-columns: 1fr;
      align-items: start;
      gap: 40px;
      padding-top: 40px;
    }
    .hero-visual {
      width: 100%;
      max-width: 520px;
      justify-self: center;
    }
    .about-inner {
      grid-template-columns: 1fr;
      gap: 32px;
      padding: 64px var(--container-pad);
    }
    .projects-grid {
      grid-template-columns: 1fr;
    }
    .projects {
      padding-top: 72px;
      padding-bottom: 72px;
    }
  }

  @media (max-width: 560px) {
    .hero-sub {
      max-width: none;
    }
    /* The labels are what make the constellation mean anything, and they do not
       fit here. Without them it is 250px of noise above the fold, so it goes. */
    .hero-visual {
      display: none;
    }
    /* Spread across a wrapped row leaves one orphan pushed to an edge, so the
       links close up into a left-aligned cluster instead. */
    .foot-nav {
      justify-content: flex-start;
      gap: 16px 28px;
      margin-bottom: 48px;
    }
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    /* Half the column width here, so the word-value drops another step rather
       than wrapping and pushing its label off the row's line. */
    .stat-value--words {
      font-size: 18px;
    }
    /* Re-draw dividers for a 2-col grid: reset all, then border 3rd/4th on top. */
    .stat {
      border-left: none;
      padding-left: 0;
      padding-right: 0;
    }
    .stat:nth-child(even) {
      border-left: 1px solid var(--border-1);
      padding-left: 24px;
    }
    .stat:nth-child(n + 3) {
      border-top: 1px solid var(--border-1);
    }
    .footer-top {
      padding-top: 88px;
      padding-bottom: 72px;
    }
    .footer-bar-inner {
      flex-direction: column;
      gap: 8px;
    }
  }
</style>
